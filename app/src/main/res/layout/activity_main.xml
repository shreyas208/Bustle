<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#eee"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Card">

        <EditText
            android:id="@+id/searchField"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingStart="@dimen/content_padding_default"
            android:paddingEnd="@dimen/content_padding_default"
            android:hint="@string/search_for_stop"
            android:drawableEnd="@drawable/ic_search_24px"
            android:inputType="textAutoComplete"
            android:imeOptions="actionSearch"/>

    </RelativeLayout>

    <TextView
        android:id="@+id/text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Card" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/nearbyRecycler"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        style="@style/Card"/>

</LinearLayout>